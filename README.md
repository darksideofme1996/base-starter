# Base Starter Project

Minimal example for deploying a contract on Base using Hardhat.

## Setup

```bash
npm install
npx hardhat run scripts/deploy.js --network baseSepolia
PRIVATE_KEY=
RPC_URL_BASE_SEPOLIA=https://sepolia.base.org
